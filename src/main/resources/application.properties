#spring.application.name: demo-config
logging.config=classpath:logback.xml

# the options from org.apache.camel.spring.boot.CamelConfigurationProperties can be configured here
# camel.springboot.name=MyCamel

# lets listen on all ports to ensure we can be invoked from the pod IP
#server.address=0.0.0.0
#management.address=0.0.0.0

# lets use a different management port in case you need to listen to HTTP requests on 8080
#management.port=8081
management.endpoint.web.exposure.include = info,refresh,keepalive,health

# disable all management enpoints except health
#endpoints.enabled = false
#endpoints.health.enabled = true
camel.component.servlet.mapping.context-path=/*

spring.artemis.host=demo-broker-ss-0.demo-broker-hdls-svc.kafka.svc.cluster.local
spring.artemis.port=5672
spring.artemis.user=admin
spring.artemis.password=admin
spring.cloud.kubernetes.reload.enabled=true
queue.demoQueue=jms:queue:demoQueue::demoQueue
